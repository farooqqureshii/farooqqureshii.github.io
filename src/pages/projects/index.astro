---
import { SITE_DESCRIPTION } from "../../consts";
import Layout from "../../layouts/Layout.astro";

const projects = [
  {
    title: "uOttaHack | Director",
    description: "Leading the team behind Ottawa's largest tech event",
    media: "/static/uottahack.jpg",
    category: "product",
    link: "https://www.uottahack.ca/",
    mediaType: "image",
    technologies: ["PM", "UI/UX", "Figma", "Software", "Sales"],
  },
  {
    title: "Substack Case Study",
    description: "Product/strategy case study for Substack growth",
    media: "/static/Marketing_Spaces.png",
    category: "product",
    link: "/writing/substack/",
    mediaType: "image",
    technologies: ["PM", "Strategy", "UI/UX", "Marketing"],
  },
  {
    title: "uoee.rocks",
    description: "Course visualizer and search, for uOttawa EE students",
    media: "/static/uoee.rocks-1755366145491.webm",
    category: "software",
    link: "https://uoee.rocks/",
    github: "https://github.com/farooqqureshii/uoee.rocks",
    mediaType: "video",
    technologies: ["React", "TypeScript", "Tailwind", "Framer Motion"],
  },
  {
    title: "okbutpitchit",
    description: "Code to Pitch-Ready slides, in an Instant",
    media: "/static/okbutpitchit_vid.webm",
    category: "software",
    link: "https://okbutpitchit.vercel.app/",
    github: "https://github.com/farooqqureshii/okbutpitchit",
    mediaType: "video",
    technologies: ["Next.js", "React", "Tailwind", "Node", "Llama 3"],
  },
  {
    title: "Voltus",
    description: "RLC circuit visualization, analysis, and simulation",
    media: "/static/voltus.webm",
    category: "software",
    link: "https://voltus.streamlit.app/",
    github: "https://github.com/farooqqureshii/voltus",
    mediaType: "video",
    technologies: ["Streamlit", "Plotly", "NumPy", "SciPy", "Pandas"],
  },
  {
    title: "Code Compass",
    description:
      "Understand any GitHub repo and find beginner-friendly issues, fast",
    media: "/static/codecompass_vid.webm",
    category: "software",
    link: "https://trycodecompass.vercel.app/",
    github: "https://github.com/farooqqureshii/code-compass",
    mediaType: "video",
    technologies: [
      "Next.js",
      "TypeScript",
      "Tailwind",
      "Groq Llama 3",
      "GitHub REST API",
    ],
  },
  {
    title: "EchoScope",
    description:
      "Use AI to uncover sentiment, bias, and diversity in YouTube comments",
    media: "/static/echoscope_vid.webm",
    category: "software",
    link: "https://echoscope.vercel.app/",
    github: "https://github.com/farooqqureshii/echoscope",
    mediaType: "video",
    technologies: ["TypeScript", "Node", "TensorFlow", "Sentiment Analysis"],
  },
  {
    title: "Analyzing and predicting housing prices in London",
    description: "Smarter housing price predictions with machine learning",
    media: "/static/london_vid.webm",
    category: "software",
    link: "https://farooq.craft.me/write-up",
    mediaType: "video",
    technologies: ["Pandas", "Matplotlib", "Scikit-learn", "SHAP", "LightGBM"],
  },
  {
    title: "NLP for Google Calendar",
    description: "Conversational AI for Google Calendar using NLP",
    media: "/static/nlp_vid.webm",
    category: "software",
    mediaType: "video",
    technologies: ["LangChain", "Rasa", "Typescript", "OAuth"],
  },
  {
    title: "Notion2Todoist",
    description: "Syncing Tasks from Notion to Todoist",
    media: "/static/notion api video.webm",
    category: "software",
    mediaType: "video",
    technologies: ["REST API", "Bash", "Scripting", "Python"],
  },
];

const categories = [
  { label: "Software", value: "software" },
  { label: "Product", value: "product" },
];
---

<Layout title="Farooq Q. | Projects" description={SITE_DESCRIPTION}>
  <div class="no-link-underline">
    <nav class="filter-nav mb-8 flex items-center gap-2 text-sm" aria-label="Filter projects">
      {
        categories.map((category, i) => (
          <>
            {i > 0 && <span class="filter-sep">/</span>}
            <button type="button" class="filter-link" data-tab={category.value}>
              {category.label}
            </button>
          </>
        ))
      }
    </nav>

    <div class="space-y-10">
      {
        projects.map((project, index) => (
          <div
            class="project-item space-y-2.5"
            data-type={project.category}
          >
            <div class="media-section w-full overflow-hidden rounded-lg border border-zinc-200/80 bg-zinc-100 dark:border-zinc-800/80 dark:bg-zinc-900/60">
              {project.mediaType === "video" ? (
                <video
                  src={index < 2 ? project.media : undefined}
                  data-src={index < 2 ? undefined : project.media}
                  muted
                  loop
                  playsinline
                  autoplay={index < 2}
                  preload={index < 2 ? "metadata" : "none"}
                  class={`aspect-[16/9] w-full object-cover ${index < 2 ? "priority-video" : "lazy-video"}`}
                >
                  Your browser does not support the video tag.
                </video>
              ) : (
                <img
                  src={project.media}
                  alt={project.title}
                  loading={index < 2 ? "eager" : "lazy"}
                  class="aspect-[16/9] w-full object-cover"
                />
              )}
            </div>
            <div class="space-y-1 text-sm pt-0.5">
              <h2 class="font-medium text-zinc-900 dark:text-zinc-100 text-sm">{project.title}</h2>
              <p class="leading-relaxed text-zinc-500 dark:text-zinc-400">
                {project.description}
              </p>
              <div class="project-links flex flex-row flex-wrap gap-x-4 gap-y-0 pt-1 text-sm">
                {project.github && (
                  <a
                    href={project.github}
                    target="_blank"
                    rel="noopener"
                    class="project-link"
                  >
                    GitHub
                  </a>
                )}

                {project.link && (
                  <a
                    href={project.link}
                    target={project.link.startsWith("/") ? undefined : "_blank"}
                    rel={project.link.startsWith("/") ? undefined : "noopener"}
                    class="project-link"
                  >
                    Link
                  </a>
                )}
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>

  <style>
    .filter-nav {
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .filter-link {
      font-size: 0.875rem;
      color: #71717a;
      background: none;
      border: none;
      padding: 0;
      cursor: pointer;
      transition: color 150ms cubic-bezier(0.23, 1, 0.32, 1);
    }
    html.dark .filter-link {
      color: #a1a1aa;
    }
    @media (hover: hover) and (pointer: fine) {
      .filter-link:hover {
        color: #111111;
      }
      html.dark .filter-link:hover {
        color: #ffffff;
      }
    }
    .filter-link.is-active {
      color: #111111 !important;
      font-weight: 500;
    }
    html.dark .filter-link.is-active {
      color: #ffffff !important;
    }
    .filter-sep {
      font-size: 0.875rem;
      color: #e4e4e7;
      user-select: none;
    }
    html.dark .filter-sep {
      color: #3f3f46;
    }
    .project-link {
      color: #71717a;
      text-decoration: none;
      transition: color 150ms cubic-bezier(0.23, 1, 0.32, 1);
    }
    html.dark .project-link {
      color: #a1a1aa;
    }
    @media (hover: hover) and (pointer: fine) {
      .project-link:hover {
        color: #111111;
      }
      html.dark .project-link:hover {
        color: #ffffff;
      }
    }
  </style>

  <script>
    function initProjects() {
      const tabs = Array.from(document.querySelectorAll(".filter-link"));
      const cards = Array.from(document.querySelectorAll(".project-item"));

      if (!tabs.length || !cards.length) return;

      const imgEls = Array.from(document.querySelectorAll(".project-item img"));
      const placeholder =
        "data:image/svg+xml;charset=utf-8," +
        encodeURIComponent(`
          <svg xmlns="http://www.w3.org/2000/svg" width="1200" height="800" viewBox="0 0 1200 800">
            <defs>
              <linearGradient id="g" x1="0" x2="1" y1="0" y2="1">
                <stop offset="0" stop-color="#f4f4f5"/>
                <stop offset="1" stop-color="#e4e4e7"/>
              </linearGradient>
            </defs>
            <rect width="1200" height="800" fill="url(#g)"/>
            <rect x="60" y="60" width="1080" height="680" rx="28" fill="rgba(0,0,0,0.04)"/>
            <text x="100" y="160" font-family="Inter, -apple-system, BlinkMacSystemFont, Segoe UI, sans-serif" font-size="42" fill="rgba(0,0,0,0.35)">
              Preview unavailable
            </text>
          </svg>
        `);

      imgEls.forEach((img) => {
        img.addEventListener(
          "error",
          () => {
            if ((img as HTMLElement).dataset.fallbackApplied) return;
            (img as HTMLElement).dataset.fallbackApplied = "1";
            (img as HTMLImageElement).src = placeholder;
            (img as HTMLElement).style.opacity = "0.92";
          },
          { once: false },
        );
      });

      const setActive = (name: string) => {
        tabs.forEach((tab) => {
          const isAct = (tab as HTMLElement).dataset.tab === name;
          tab.classList.toggle("is-active", isAct);
        });

        cards.forEach((card) => {
          const show = name === "all" || (card as HTMLElement).dataset.type === name;
          (card as HTMLElement).style.display = show ? "block" : "none";
        });
      };

      tabs.forEach((tab) => {
        tab.addEventListener("click", () => {
          const tabName = (tab as HTMLElement).dataset.tab;
          if (tabName) setActive(tabName);
        });
      });

      setActive("software");

      const lazyVideos = document.querySelectorAll(".lazy-video");
      const videoObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              const video = entry.target;
              if (video instanceof HTMLVideoElement) {
                const src = video.getAttribute("data-src");
                if (src && !video.src) {
                  video.src = src;
                  video.autoplay = true;
                  video.load();
                  videoObserver.unobserve(video);
                }
              }
            }
          });
        },
        { rootMargin: "200px 0px", threshold: 0 },
      );

      lazyVideos.forEach((video) => {
        if (video.classList.contains("lazy-video")) {
          videoObserver.observe(video);
        }
      });
    }

    document.addEventListener("astro:page-load", initProjects);
    initProjects();
  </script>
</Layout>
